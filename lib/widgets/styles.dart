import 'package:flutter/material.dart';

const Color defaultColor = Color(0xFF80CBC4);
const Color white = Colors.white;
const Color highlightColor = Color(0xFFF57F17);

const titleText = TextStyle(
    color: highlightColor,
    fontWeight: FontWeight.w800,
    fontFamily: 'Fryo',
    fontSize: 30,
    letterSpacing: 5);
const hightlightText =
    TextStyle(color: highlightColor, fontFamily: 'Fryo', fontSize: 20);
const blackText =
    TextStyle(color: Colors.black, fontFamily: 'Fryo', fontSize: 20);
const whiteText =
    TextStyle(color: Colors.white, fontFamily: 'Fryo', fontSize: 18);
const whiteText20 = TextStyle(
    color: Colors.white, fontFamily: 'Fryo', fontSize: 20, letterSpacing: 5);
const disabledText =
    TextStyle(color: Colors.black, fontFamily: 'Fryo', fontSize: 14);

const alertText = TextStyle(
    color: Colors.red,
    fontSize: 14,
    fontWeight: FontWeight.w500,
    fontFamily: 'Fryo');
